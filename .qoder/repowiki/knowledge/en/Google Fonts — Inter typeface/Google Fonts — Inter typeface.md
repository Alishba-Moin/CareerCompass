---
kind: external_dependency
name: Google Fonts — Inter typeface
slug: google-fonts-inter
category: external_dependency
category_hints:
    - vendor_identity
scope:
    - '**'
source_files:
    - index.html
---

The Inter font family (weights 300–900) is pulled from Google Fonts (`fonts.googleapis.com`) and set as the site's sans-serif font via both the stylesheet link and an inline Tailwind config extension. The project has no local font assets.